#include<bits/stdc++.h>
using namespace std;

int main(){
    vector<int> v;

    v.push_back(1);
    v.push_back(2);
    v.emplace_back(5);

    vector<pair<int,int>>vec;

    v.emplace_back(1,2);
    // v.push_back({1,2});

    vector<int> v(5,20);
    v.push_back(3);




}